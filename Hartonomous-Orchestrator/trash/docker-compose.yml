version: '3.8'

services:
  # Hartonomous Orchestrator
  orchestrator:
    build: .
    ports:
      - "8080:8080"   # API port
      - "9090:9090"   # Metrics port
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs
    depends_on:
      - qdrant
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Qdrant Vector Database
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    environment:
      - QDRANT__SERVICE__HTTP_PORT=6333
      - QDRANT__SERVICE__GRPC_PORT=6334
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Example: Local AI services (uncomment and configure as needed)
  # embedding-service:
  #   image: your-embedding-service:latest
  #   ports:
  #     - "8711:8711"
  #   environment:
  #     - MODEL=text-embedding-3-large
  #   restart: unless-stopped

  # generative-service:
  #   image: your-generative-service:latest
  #   ports:
  #     - "8710:8710"
  #   environment:
  #     - MODEL=gpt-4
  #   restart: unless-stopped

  # reranking-service:
  #   image: your-reranking-service:latest
  #   ports:
  #     - "8712:8712"
  #   environment:
  #     - MODEL=rerank-lite
  #   restart: unless-stopped

volumes:
  qdrant_data:
    driver: local

networks:
  default:
    name: hartonomous-network